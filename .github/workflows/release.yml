name: Latest Release Workflow

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "something new"
          echo '${{ toJSON(github.event) }}' | jq
          
